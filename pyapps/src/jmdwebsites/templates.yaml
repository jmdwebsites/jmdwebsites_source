pages:
    empty:
        layouts: empty
    default:
        layouts: default
        partials: default
        vars: default
    page:
        inherit: default
    about:
        inherit: default
        layouts: about
layouts:
    empty:
        doc:
    default:
        inherit:
        doc:
            doctype:
            html:
        html:
            head:
            body:
        head:
            charset:
        body:
            header:
            main:
            footer:
        header:
        main:
            article:
    page:
        inherit: default
        main:
            headlines:
            article:
    about:
        inherit: page
        main:
            cards:
            article:
partials:
    html5:
        doctype: '<!DOCTYPE html>'
        html: '<html lang="{lang}">{block}</html>'
        head: '<head>{block}</head>'
        charset: '<meta charset="{charset}"/>'
        body: '<body>{block}</body>'
        header: '<header></header>'
        main: '<main>{block}</main>'
        footer: '<footer></footer>'
        article: '<article></article>'
        cards: '<div class="cards"></div>'
    default:
        inherit: html5
    html4:
        inherit: html5
        doctype: '<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN">'
vars:
    default:
        lang: 'en'
        charset: 'utf-8'
